"const fs = require('fs'); const en = JSON.parse(fs.readFileSync('src/dictionaries/en/tools.json', 'utf8')); const fa = JSON.parse(fs.readFileSync('src/dictionaries/fa/tools.json', 'utf8')); const enKeys = Object.keys(en); const faKeys = Object.keys(fa); const missing = enKeys.filter(k => !faKeys.includes(k)); console.log(missing.length > 0 ? 'Missing: ' + missing.join(', ') : 'All present.');"  
